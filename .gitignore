
# Supprimer les éléments dans Git
# git filter-branch --force --index-filter "git rm --cached --ignore-unmatch path/to/huge/file/with/error" --prune-empty --tag-name-filter cat -- --all

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Ignore lock files
*.lock.hcl

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc
gitkraken-amd64.deb
